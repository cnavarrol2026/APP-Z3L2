# Cambios

## 2026-07-17

- Se implementa estructura inicial del proyecto.
- Se agrega documentación maestra.
- Se agrega base Apps Script modular.
- Se define base vacía y acceso con cuenta Google.
- Se define PDF sin imágenes.
- Se define respaldo ZIP con CSV/JSON.
- Se crea inicialmente Google Sheets nativo y carpetas Drive.
- Se detecta que los recursos quedaron en cuenta Gmail y se eliminan antes de crear Apps Script.
- Se revierte `src/Config.gs` a placeholders corporativos para recrear recursos desde `cnavarrol@vspt.cl`.
- Se crea Google Sheets corporativo y Apps Script ligado con `clasp` autenticado como `cnavarrol@vspt.cl`.
- Se ejecuta inicialización manual desde el editor Apps Script corporativo.
- Se crean/registran carpetas Drive desde el propio script corporativo.
- Se crea despliegue web inicial de Apps Script.
